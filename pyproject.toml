[build-system]
requires = ["hatchling>=1.18"]
build-backend = "hatchling.build"

[project]
name = "radivault-gateway"
version = "0.1.0"
description = "RadiVault Gateway Agent + Central Ingest — on-prem PACS → De-ID → Central uploader and Zone 2 ingest service"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Proprietary" }
authors = [{ name = "RadiVault" }]
dependencies = [
    "click>=8.1",
    "httpx>=0.27",
    "pydantic>=2.6",
    "pydicom>=2.4",
    "pyyaml>=6.0",
    # metadata-thumbnail-ingest FR-THUMB-1: ingest-time JPEG generation.
    "Pillow>=10.0",
    "numpy>=1.26",
]

[project.optional-dependencies]
mock = [
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "python-multipart>=0.0.9",
]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.5",
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "python-multipart>=0.0.9",
]
central = [
    # Core web stack
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "gunicorn>=21.2",
    "python-multipart>=0.0.9",
    # Persistence
    "sqlalchemy>=2.0",
    "alembic>=1.13",
    "psycopg[binary]>=3.1",
    # Cache / idempotency / rate limit
    "redis>=5.0",
    "slowapi>=0.1.9",
    # Object storage
    "boto3>=1.34",
    # Auth
    "argon2-cffi>=23.0",
    # Observability
    "prometheus-client>=0.20",
    "prometheus-fastapi-instrumentator>=7.0",
    "python-json-logger>=2.0",
    "opentelemetry-api>=1.24",
    "opentelemetry-sdk>=1.24",
    "opentelemetry-instrumentation-fastapi>=0.45b0",
    # ULIDs for request_id / job_id
    "python-ulid>=2.0",
]
central-test = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "httpx>=0.27",
    "fakeredis>=2.20",
]
search = [
    # Core web stack — share central's foundations.
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "gunicorn>=21.2",
    # Persistence.
    "sqlalchemy>=2.0",
    "alembic>=1.13",
    "psycopg[binary]>=3.1",
    # Cache / rate limit.
    "redis>=5.0",
    "slowapi>=0.1.9",
    # Auth.
    "argon2-cffi>=23.0",
    # Observability.
    "prometheus-client>=0.20",
    "prometheus-fastapi-instrumentator>=7.0",
    "python-json-logger>=2.0",
    "opentelemetry-api>=1.24",
    "opentelemetry-sdk>=1.24",
    "opentelemetry-instrumentation-fastapi>=0.45b0",
    # ULIDs.
    "python-ulid>=2.0",
    # Preview thumbnail / sample-download object storage (FR-API-1).
    # MinIO/S3 backed via boto3 — required when RV_PREVIEW_S3_ENDPOINT set.
    "boto3>=1.34",
]
search-test = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "httpx>=0.27",
    "fakeredis>=2.20",
]
# Order Fulfillment (dev-spec-order-fulfillment §9.4) — shares the central/search
# foundations but adds boto3 (presigned URLs) and reuses redis/slowapi.
fulfillment = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "gunicorn>=21.2",
    "sqlalchemy>=2.0",
    "alembic>=1.13",
    "psycopg[binary]>=3.1",
    "redis>=5.0",
    "slowapi>=0.1.9",
    "boto3>=1.34",
    "argon2-cffi>=23.0",
    "prometheus-client>=0.20",
    "prometheus-fastapi-instrumentator>=7.0",
    "python-json-logger>=2.0",
    "opentelemetry-api>=1.24",
    "opentelemetry-sdk>=1.24",
    "opentelemetry-instrumentation-fastapi>=0.45b0",
    "python-ulid>=2.0",
]
fulfillment-test = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "httpx>=0.27",
    "fakeredis>=2.20",
]
# v0.2 de-id-pixel: OCR + 3D defacing (opt-in; included in :0.2.0-pixel image).
pixel = [
    "pytesseract>=0.3.13",
    "pydeface>=2.0",
    "nibabel>=5.2",
    "numpy>=1.26",
    "Pillow>=10.3",
    "opencv-python-headless>=4.9",
]
# Optional PaddleOCR path (:0.2.0-pixel-paddle image).
pixel-paddle = [
    "paddleocr>=2.7",
    "paddlepaddle>=2.6",
]
# Demo-seed pipeline (scripts/demo_seed/*) — dev-spec buyer-portal-demo §8 FR-S-4.
# Installed only on the operator workstation that runs the one-shot TCIA fetch.
demo-seed = [
    "tcia_utils>=3.3",
    "requests>=2.32",
    "pyyaml>=6.0",
    "httpx>=0.27",
    "pydicom>=2.4",
]

[project.scripts]
radivault-gateway = "radivault_gateway.cli.main:cli"
radivault-central = "radivault_central.__main__:main"
ingest-admin = "radivault_central_admin.cli:cli"
radivault-search = "radivault_search.__main__:main"
search-admin = "radivault_search_admin.cli:cli"
radivault-fulfillment = "radivault_fulfillment.__main__:main"
fulfillment-admin = "radivault_fulfillment_admin.cli:cli"

[tool.hatch.build.targets.wheel]
packages = [
    "src/radivault_gateway",
    "src/radivault_mock_central",
    "src/radivault_central",
    "src/radivault_central_admin",
    "src/radivault_search",
    "src/radivault_search_admin",
    "src/radivault_fulfillment",
    "src/radivault_fulfillment_admin",
]

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
markers = [
    "integration: integration tests requiring external services (Orthanc, docker); skipped by default",
    "search_integration: integration tests for radivault_search; gated by SEARCH_TEST_SERVICES=1",
    "fulfillment_integration: integration tests for radivault_fulfillment; gated by FULFILLMENT_TEST_SERVICES=1",
]
filterwarnings = [
    "ignore::DeprecationWarning:pydicom.*",
]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "SIM", "RUF"]
ignore = [
    "E501",  # line length handled by formatter
    "B008",  # function call in default arg (click / FastAPI patterns)
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["B", "SIM", "E702", "RUF059", "RUF002", "RUF003"]
"alembic/**" = ["E402", "F401"]
# Fulfilment routers intentionally unpack (_, _, gateway_id) tuples for clarity
# even when the first two values aren't used — the naming documents intent.
"src/radivault_fulfillment/routers/gateway_jobs.py" = ["RUF059"]
"src/radivault_fulfillment/routers/buyer_orders.py" = ["RUF059"]
